{
  "name": "dapp-project-template-front",
  "version": "1.0.0",
  "description": "A simple UX for your smart contracts.",
  "main": "main.js",
  "author": {
    "name": "Matthieu SCARSET",
    "email": "m@matthieuscarset.com",
    "url": "https://matthieuscarset.com"
  },
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "serve": "node lib/server.js",
    "init": "yarn run clone:config && yarn run clone:contract",
    "clone:config": "cp -v -u -p src/modules/config.example.js src/modules/config.js",
    "clone:contract": "cp -R ../artifacts/contracts ./ && rm contracts/*/*.dbg.json"
  }
}
